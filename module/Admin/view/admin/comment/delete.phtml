<?php
$title = 'Delete comment';
$this->headTitle($title);
?>

<div class="row">

    <div class="span10 container well well-large">
        <h1><?php echo $this->escapeHtml($title); ?></h1>
        <h3>
            Are you sure that you want to delete
            '<?php echo $this->escapeHtml($comment->message); ?>' by
            '<?php echo $this->escapeHtml($comment->author); ?>'?
        </h3>

        <?php
        $url = $this->url('admin/comment', array(
            'action' => 'delete',
            'id'     => $this->id,
        ));
        ?>
        <form action="<?php echo $url; ?>" method="post">
            <input type="hidden" name="id" value="<?php echo (int) $comment->id; ?>" />
            <input type="submit" name="del" value="Yes" class="btn btn-large btn-success" />
            <input type="submit" name="del" value="No" class="btn btn-large btn-danger" />
        </form>
    </div>
    
</div>